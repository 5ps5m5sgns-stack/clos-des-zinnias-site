# OS / éditeurs
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# Logs / divers
*.log

# Photos drone originales (non-optimisées, ~105 Mo) — non utilisées par le site.
# Seules les versions WebP de assets-optimized/ et images-optimized/ sont servies.
assets/

# Sauvegardes locales d'audit / backups d'assets - ne pas versionner
_backup_pre_audit_*/
*.luxe-backup

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
