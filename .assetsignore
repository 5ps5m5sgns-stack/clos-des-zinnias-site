# Fichiers à NE PAS déployer publiquement (Cloudflare Workers static assets)
.git
.gitignore
.gitattributes
.assetsignore
.wrangler
wrangler.jsonc
wrangler.toml
node_modules
*.log
*.bak
*.map

# Photos drone originales non-optimisées (~105 Mo) — le site sert les WebP
assets
